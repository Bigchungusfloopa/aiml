# Core ML (classical only — SRS constraint 2.4)
scikit-learn>=1.4
pandas>=2.1
numpy>=1.26
scipy>=1.11
joblib>=1.3

# GPT-2 perplexity scoring only (not trained — SRS constraint 2.4)
transformers>=4.40
torch>=2.2

# Dataset acquisition
datasets>=2.19

# Web UI
streamlit>=1.33

# Document upload (PDF / DOCX text extraction)
pypdf>=4.2
python-docx>=1.1

# Plots for the evaluation report & Streamlit UI
matplotlib>=3.8
plotly>=5.20
