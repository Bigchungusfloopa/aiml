# Slimmed dependency set for memory-constrained hosting (e.g. Streamlit
# Community Cloud's ~1 GB limit). Drops torch + transformers, so the GPT-2
# perplexity module is disabled and the app runs on the two classical models
# plus the rule-based checker only — it degrades gracefully (NFR-3).

scikit-learn>=1.4
pandas>=2.1
numpy>=1.26
scipy>=1.11
joblib>=1.3
streamlit>=1.33
pypdf>=4.2
python-docx>=1.1
